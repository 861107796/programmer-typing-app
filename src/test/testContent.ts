export const testPrompts = {
  code: "const total = items.map((item) => item.id).join(',');",
  command: "git commit -m \"feat: add typing engine\"",
  technical: "The cache should invalidate stale entries before returning a response.",
};
